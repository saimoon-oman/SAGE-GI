# Core scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Genome I/O and benchmark parsing
biopython>=1.81
xlrd>=2.0            # reads the .xls IslandPick additional files

# Genome foundation models.
# DNABERT-2 / DNABERT-S ship custom modelling code (MosaicBERT with ALiBi) written against
# transformers 4.x; the meta-device loader introduced in transformers 5 cannot execute it.
torch>=2.0
transformers>=4.40,<5.0
tokenizers<0.21
einops>=0.6
huggingface_hub>=0.20

tqdm>=4.65
